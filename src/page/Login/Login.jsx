import { LoginForm } from 'components/Auth';
import { LoginContainer } from './Login.styled';

const Login = () => {
  return (
    <LoginContainer>
      <LoginForm />
    </LoginContainer>
  );
};
export default Login;
